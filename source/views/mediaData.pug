extend ../Layout.pug

block pageCSS
  //- link(href="./css/index.css", rel="stylesheet")

block pageJs
  script(src="./js/mediaData.js")
  
block pageType
  - var page = 'media'
  
//- block head
//-   include ../components/Header.pug

//- block footer
//-   include ../components/Footer.pug

block body
  article.media-data-page
    h1 media-data
    .btn.add-btn.btn-primary 新增
    hr
    table.table.table-striped
      thead.thead-dark
        tr
          th(scope='col') *
          th(scope='col') 標題
          th(scope='col') 描述
          th(scope='col') 出處
          th(scope='col') 連結
          th(scope='col') 時間
          th(scope='col') 照片
          th(scope='col') 是否啟用
          th(scope='col') 
      tbody







